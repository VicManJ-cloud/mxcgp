// @ts-check
import { defineConfig } from 'astro/config';

export default defineConfig({
  // Cambiar por el dominio definitivo antes de publicar.
  site: 'https://mxconsultores.mx',
  build: { format: 'directory' },
});
